{
  "name": "acm-dl-extension",
  "version": "1.0.0",
  "description": "Extensions to automate downloading from the ACM",
  "scripts": {
    "build": "npm run clean && tsc && npm run copy-files",
    "copy-files": "cp manifest.json dist/ && cp popup.html dist/ && cp styles.css dist/ && cp -r icons dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["chrome-extension", "acm", "automation"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.3.3"
  }
}
